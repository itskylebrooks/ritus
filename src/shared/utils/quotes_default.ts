// Deprecated: `quotes_default` has been consolidated into `quotes.ts` as the single source of truth.
// Keep this file as a compatibility shim for any external imports that may reference it.

export { DEFAULT_QUOTES } from './quotes';
